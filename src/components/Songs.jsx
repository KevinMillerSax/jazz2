import styled from 'styled-components';

const SongContainer = styled.div`
  width: 50%;
`;

const Songs = () => {
  return(
    <SongContainer>
      Songs
    </SongContainer>
  );
};

export default Songs;